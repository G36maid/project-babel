# Backend API Configuration
# This URL is used by the frontend to connect to the backend API
# In development, the Vite dev server will proxy requests to this URL
# In production, this should be the full URL to your backend API

# Development: local backend server
VITE_BACKEND_URL=http://localhost:3000

# Production example: deployed backend
# VITE_BACKEND_URL=https://api.yourdomain.com

# Docker example: backend service name
# VITE_BACKEND_URL=http://backend:3000
